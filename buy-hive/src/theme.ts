import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  typography: { fontFamily: "'Avenir LT Pro', Avenir, Arial, sans-serif" },
  palette: { primary: { main: '#00b2c9' }, secondary: { main: '#29b574' } },
  shape: { borderRadius: 4 },
})
